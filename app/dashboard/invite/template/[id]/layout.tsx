export default function TemplateLayout({
    children
                                       } : {
    children: React.ReactNode;
}){
    return (
        <div>
            {children}
        </div>
    )
}